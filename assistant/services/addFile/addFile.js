
var config = require('./../../config.json');

var logger = require(config.root + '/utils/logger');
var comm = require(config.root + '/utils/communicator');



var addFile = function(options){
    
};

module.exports = {
    addFile: addFile
};